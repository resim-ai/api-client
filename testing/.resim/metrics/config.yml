version: 1
topics:
  ok:
    schema:
      speed: float
metrics:
  Average Speed:
    type: test
    query_string: SELECT AVG(speed) FROM speed WHERE job_id=$job_id
    template_type: system
    template: line
metrics sets:
  woot:
    metrics:
      - Average Speed